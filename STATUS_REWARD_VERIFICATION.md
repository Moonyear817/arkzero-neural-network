# 奖励机制验证版 — 保存并暂停

2026-09-15。本轮修改训练目标、搜索回传、编队学习和训练课程。尚未进行正式训练，不代表已经学会部署或通关。原有战斗机制的还原范围仍以 `STATUS_MECHANICS_VERIFICATION.md` 为准。

## 新规则

整局分数 = 结束奖励 + 0.2 × 击杀比例 − 0.2 × 蓝门生命损失比例。

| 结束情况 | 结束奖励 |
| --- | ---: |
| 全歼且零漏怪通关 | +0.6 |
| 按底层原版胜负规则通关，但有漏怪 | +0.2 |
| 正常战败 | −0.6 |

零漏怪完整通关总分为 +0.8。普通通关的分数高于正常战败，但始终低于零漏怪通关。失败中的每次有效击杀仍能增加分数，不再全部被截断为 −1。蓝门损失按实际扣血比例计算，因此扣 2 点生命的敌人与扣 1 点的敌人不同。

过程分使用相邻状态的击杀/生命变化，整局累计等于最终进度减去初始进度。部署、撤退、按机关、等待、累计伤害、耗费费用本身不奖励，也没有生存时间奖励；反复操作不能凭次数赚分。合法撤退未被删除或统一惩罚。机关的收益来自它实际帮助造成的战况变化。

战斗网络学习从当前状态开始的剩余收益，已经发生的得失不会反复算到之后的每个动作上。MCTS 使用同一奖励累加回传；编队网络使用同一整局目标。所有正常剩余收益都在网络输出的 [-1, 1] 范围内，未使用会压平早期进步的最终奖励截断。

时间或决策次数达到训练上限时，标记为“截断”：不向战斗经验池加入该局，也不更新编队网络。搜索对这类边界使用整局 −0.8 的保守评分，使拖延到上限不会比自然战败占优；这不是正常战败标签，不参与价值监督。截断评估不能晋升为最佳模型。人工停止的未完成局同样被丢弃。

## 学习与搜索的调整

- 搜索遇到只有一个合法动作的新状态，会在同次模拟中继续向前，仍受最大深度限制。没有加入预设部署位置或固定打法。
- 经验批次在可用样本足够时优先包含 80% 真正有选择的状态。只有一个动作的状态不贡献、也不稀释策略损失，仍可用于价值学习。
- 自主编队默认交替训练：奇数轮训练战斗，偶数轮训练编队；另一张网络冻结。编队仍由网络自主采样，没有人工答案。
- 自我对战使用当前候选模型，保留最佳模型供固定评估比较。评估排序依次为零漏怪率、普通通关率、整局分数、较少漏怪、较多击杀。
- 新配置先训练 0-1；当前已开放课程在固定评估中连续 3 次达到 80% 零漏怪率，才逐步解锁 0-8、0-9。评估始终覆盖同一组关卡，每关 3 个固定种子，共 9 局，以保持可比性。重复固定种子不代表独立泛化证明。
- 新建网络的价值输出从 0 开始，避免未训练就出现极端预测。新增日志包括奖励范围、普通通关率、截断比例、实际更新次数和刚部署就撤退次数。

3-7 及更复杂的机关关卡没有自动加入训练课程，需要先完成对应机制验证。课程只解锁配置中列出的关卡，不会擅自开放尚未还原的内容。

## 使用和保存

重新打开应用，训练页的高级配置列表中可加载“奖励机制验证 · 自主编队”（`configs/reward_verification.yaml`）。这只加载配置，不会自动开始。正式训练需要用户在验证后启动。

模拟器优先显示 `outputs/reward_verification/initial.pt`，界面明确标记“奖励机制验证模型（尚未训练）”。它包含全新战斗网络和 77 人候选池中的自主 6 人编队网络，可验证识别、合法操作及接线，不能作为策略成绩。

- `outputs/reward_verification/ready_to_train.pt`：第 0 轮完整准备存档，经验池为空，可在批准开始后继续。
- `outputs/reward_verification/status.json`：PAUSED_FOR_VERIFICATION，training_started=false。
- `outputs/reward_verification/source_snapshot.zip`：源代码、配置和说明的冻结副本。
- `outputs/reward_verification/saved_manifest.json`：源文件及存档的 SHA-256。
- `logs/reward_v2_tests.log`：自动回归结果。
- `outputs/reward_verification/runtime_verification.json` 与 `desktop_verification.png`：通过桌面人机协同控件加载自主编队、执行一个决策并关闭模拟的记录。

旧奖励存档、优化器和经验池不能直接混入新训练。显式通过 warm_start 导入旧模型时保留特征与策略，但重置不兼容的最终价值层和编队价值基线；旧源文件不变。应用加载旧模型用于新规则搜索时保留其策略，旧价值预测按中性 0 处理，避免把旧评分含义当作新评分。

上一轮 `outputs/overnight_joint` 仍为停止状态，机制验证版存档仍保留。新训练将写入独立的 `outputs/reward_learning`，不会覆盖旧训练记录。

## 验证范围

自动检查覆盖奖励等级、失败改善、不同漏怪扣血、部署撤退不刷分、逐步回报与搜索回传一致、截断不更新任一网络、交替更新隔离、课程晋级及恢复、旧存档隔离、桌面状态区分，以及原有战斗/界面回归。

完整自动回归通过 305 项（50.87 秒）。测试中的短小参数更新仅用于验证学习代码，测试模型不作为交付模型；交付的 initial.pt 和 ready_to_train.pt 未经训练。

本轮不以误差下降或随机网络输出合法动作作为策略进步证明。后续仍需在相同关卡和种子上比较零漏怪率、普通通关率、漏怪、立即撤退及截断数量，并使用额外种子测试泛化。奖励权重是有界的初始方案，需要这些结果来决定是否调整。
