> 最新实现与逐关缺口见 STATUS_CHAPTER8_FOUNDATIONS.md：账号已确认，R8-1 基础战斗可运行，其余关卡和全近卫训练尚未达到验收条件。

# 账号近卫 × 第八章：实施进度

## 视频账号录入更新（2026-09-15 晚）

视频已保存并整理为379名可见干员、71名近卫，包含专精及装备模组；信赖按用户要求统一200%。账号仍为待人工确认草稿，近卫仅乌尔比安潜能待确认。模组与信赖资料表已补齐，之前“尚无截图/模组表”的缺口已变化。当前完整测试420 passed。

本阶段仍未完成，未开始正式训练。最新数据、验证和具体阻碍以 `STATUS_ACCOUNT_VIDEO.md` 及 `outputs/guards_chapter8/account_video/training_preflight.json` 为准；下文保留前一轮实施记录。


2026-09-15。**计划尚未完成；正式训练未开放、未启动。** 本轮完成账号截图导入、人工确认快照、准入检查与实际关卡缺口审计。没有连接 PlayCover。

## 本轮已实现

- `accounts/ocr.py` 与 `recognize.swift`：使用本机 Apple Vision 识别图片文字，保留置信度与位置。精确姓名匹配，单干员截图中的明确文字字段生成候选；缺失、冲突、低置信度字段保持 null。潜能图标、各技能专精、装备模组尚需人工录入。
- `accounts/snapshot.py`：校验近卫身份、实际等级、潜能提升次数0–5、信赖0–200、技能等级和专精解锁条件；必须人工确认。内容哈希作为版本ID，重复保存不覆盖旧快照，读取校验完整性。未装备模组明确填写 `{"id":"","level":0}`。
- 训练页 → 高级配置 → “账号截图与核对…”：后台OCR、表格编辑、原文证据、导入/保存草稿和确认快照；选中的快照路径随训练配置保存。复杂字段暂以JSON编辑，后续应改成技能/模组选择器。重复截图不会静默覆盖，重复干员需核对并删除多余行。
- `accounts/readiness.py`：只审计R8/M8/JT8普通战斗，实际调用当前Loader检查，而不是信任旧目录标签；输出每关的当前首个阻碍。此清单不是所有缺失机制的穷尽清单。
- 正式范围 `training_scope: account_guards_chapter8` 在创建模型、目录和开始训练前检查账号与机制。当前未完成完整特性/天赋校准的候选均明确拒绝，不将普通攻击近似视为支持。
- `configs/guards_chapter8.yaml`：R8-1、16次搜索、单线程、有界1轮1局配置，等待真实账号快照。空队伍表示待配置，不自动替换默认队伍。
- CLI：`scripts/import_account.py recognize IMAGE --output DRAFT.json`；人工核对后使用 `confirm DRAFT.json --confirmed-by NAME` 保存快照。`scripts/audit_guard_curriculum.py --output REPORT.json` 生成就绪报告。

## 验证

- 检查到现有源码已有暂停后更新，召唤物先攻击后致死测试与同时间致死取消攻击测试均已存在，无需重复改写。
- 修改前完整回归：400 passed，66.67秒。证据：`research/skills/tests_guard_baseline.xml`。
- 首轮修改后完整回归：414 passed，68.13秒。证据：`outputs/guards_chapter8/tests.xml`。
- 后续补充桌面相对快照路径和空队伍配置测试：账号专项15 passed。最后桌面/账号定向测试结果见 `outputs/guards_chapter8/tests_account_desktop.xml`。
- 实际运行Apple Vision识别合成测试图成功：`outputs/guards_chapter8/synthetic_ocr_draft.json`。姓名识别成功，等级/信赖低置信度保持未知；这不是对真实游戏截图准确率的证明。
- `scripts/run_desktop.py --quit-after 4` 退出码0；系统有IMK输入法日志，没有导致退出失败。尚未使用真实账号截图验证完整人工确认流程。

## 仍需完成

1. 用户提供真实账号近卫截图并确认资料，才能确定所需特性、天赋、技能和模组集合；没有编造账号持有或练度。
2. 当前缺少 `uniequip_table.json`，已装备模组的确认会报告资料缺失，不能猜测合法性或忽略模组。
3. 信赖属性、完整近卫分支/天赋/技能/模组效果需按账号逐项实现验证。当前“未就绪”判定是保守关闭状态，不是完整的可动态开放能力注册系统。
4. R8-1真实配置包含5个 `trap_020_roadblock` 预置路障，是当前首个解析缺口；必须查明其可受击、移除、阻挡/路径语义，不能直接当普通箱子使用。
5. 第八章其他路线、装置、预置单位、敌人及保护目标尚未完成；没有把全部地图标为可训练。
6. 现有网络仍包含未来窗口、波次图、路线信息；单独 `future_events_enabled=false` 不能消除全部泄漏。必须实现并验证玩家可见观测，当前正式训练因此保持关闭。
7. 账号各干员异构练度绑定、完整自主选队和携带技能选择、课程回顾以及快照与checkpoint的强一致绑定尚未完成。
8. 真实关卡3个训练种子×每模型20局评估、通关时间轴和严格新实例重放尚未运行。不以本轮基础设施测试宣称学会通关。

下一步先补截图真实样本与模组来源，同时推进R8-1路障及敌人机制审计。当前没有后台长期训练任务由本轮启动。
