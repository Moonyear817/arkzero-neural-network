> 2026-09-15 更新：现已加入职业编队、三种控制、本地全地图和自主编队训练。最新范围与操作见 [STATUS_MAPS_JOINT.md](STATUS_MAPS_JOINT.md)。

# PRTS 全员资料与基础模拟接入

导入日期：2026-09-15。来源：[PRTS 干员一览](https://prts.wiki/w/干员一览)。

## 打开新版

双击 `dist/Arknights Zero PRTS.app`。已验证启动、自动加载资料库与安全退出，并通过本机签名校验。此入口直接使用当前工作区的代码、数据和 `.venv`，后续更新即可生效；请保留在项目的 `dist` 目录内。它是本机启动版，不是可移走的独立安装包。旧版 `dist/Arknights Zero.app` 保留。

可用 `scripts/build_prts_local_app.py` 重建入口。完整独立包编译已停止，未作为本次交付；`pysidedeploy_prts.spec` 保留供以后构建使用。

## 已交付能力

- 列表中的 **431 名干员全部收录**，包括阿米娅术师、近卫、医疗三种形态；431 个页面均取得原始文本、修订号与修订时间，全部匹配游戏内部 ID，没有未匹配条目。
- 资料库保存列表全部字段、页面完整 wikitext；保留中/英/日名称、编号、职业、分支、标签、获取方式、特性等。结合固定 GameData 提供所有精英阶段属性、技能全部等级与天赋数据；原文中档案、语音台词等文本同样保留。图片、音频文件、动画和链接指向的其他页面不在本次文字资料导入范围内。
- 「游戏数据 → 干员」可搜索并查看易读摘要、完整 PRTS 原文或原始游戏/技能数据。PRTS 列表数值与默认战斗属性可能不同：列表展示高等级属性，默认战斗使用 E0 Lv1、潜能1、信赖0。
- 「模拟器 → 全员编队…」选择最多 12 人，自动切换手动控制。开始后用单步推进费用，再通过合法动作部署、选方向、撤退及开启已支持的技能。随机智能体也可使用该队伍。
- 「训练 → 全员编队…」采用同一资料库，保存配置时保留全员选择；新干员可进入 self-play、PUCT、模型训练与存档。模型默认容量扩至 12 人，输入特征与模型权重的维度保持兼容；原来未使用的第 25 号特征区分治疗/不普攻支援。
- 中文名、英文名、日文名和内部编号可用于识别。存在多个形态的共同外文名（如 Amiya）明确报歧义，使用中文形态名或唯一内部编号即可消除歧义。

## 战斗还原范围

全部干员可加载和部署，不等于全部机制已经复刻。默认配置为精英0、1级、一技能、技能等级1。API 还支持精英阶段和等级选择，基础成长属性线性插值；此扩展未获得逐帧实机校准。

目前提供基础单体物理/法术攻击与医疗单体治疗。吟游者、工匠和解放者不执行错误的普通攻击。53 名干员的一技能可执行共享的攻击/防御/攻速强化；其他技能资料完整保留，但战斗效果明确禁用。召唤物、模组、天赋、溅射、连击、元素损伤、特殊阻挡、部署数例外等没有完整实现，所有非历史基线干员标记为基础近似模拟。不要将其对局成绩解释为真实客户端的通关保证。

旧模型的读取兼容不意味着旧模型已经学会全员；已完成一轮包含阿米娅、能天使、安赛尔、黑角的独立训练冒烟检查，仅验证数据→动作→训练→存档链路，不代表完成策略训练或达到通关水平。原有已发布模型没有被替换。

## 数据与复现

- `data/real/operator_catalog.json`：431 人的统一资料库、别名、来源、网页完整原文。
- `data/prts/operator_list.html`、`data/prts/pages.json`：可断点续传的原始缓存。
- `data/real/char_patch_table.json`：与现有 GameData 同一固定提交的职业形态数据，来源与 SHA-256 已补入 `data/manifest.json`。
- `scripts/import_prts_operators.py`：默认复用缓存；加 `--refresh` 重新抓取。批量 20 页，有限重试，顺序请求。
- `configs/prts_smoke.yaml`、`outputs/prts_smoke/`：独立的小型训练验证配置与输出。
- `research/prts_runtime/`：真实桌面窗口的资料搜索、全员编队、能天使部署截图与检查结果。

版权归属及 PRTS CC BY-NC-SA 4.0 内容许可见 `THIRD_PARTY_NOTICES.md`。只导入本地工作区，没有发布或推送。

## 验证

- 全部 431 人逐一加载、部署、撤退、状态隔离与模型特征编码通过。
- 中外文识别、阿米娅职业区分、成长等级边界、未知技能禁用、医疗治疗对象测试通过。
- 12 人合法动作与神经策略概率输出通过。
- 完整回归 244 项通过；资料展示收尾后相关桌面测试 21 项再次通过。
- 原生桌面实测：431 人资料可见，三种阿米娅可搜索，选编队并手动部署能天使成功。
- 新队伍完成 self-play、一次参数更新、评估与 checkpoint 保存。短验证采用决策次数上限，不作通关成绩验收。

- 训练页保存/重载新干员编队的独立回归通过。
- 53 个已支持的一技能逐一实际开启验证通过，清单见 `research/prts_runtime/coverage.json`。
