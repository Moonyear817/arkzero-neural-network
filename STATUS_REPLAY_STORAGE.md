# 训练样本独立存储：修改与验证

模型和训练样本已经分开保存。默认桌面训练的样本统一放在 `训练数据/`，
按任务分子文件夹；模型参数、优化器、训练进度继续保存在模型存档中。
应用启动器直接读取本项目代码，重新打开应用即可使用新逻辑。

## 已完成

- 新存档使用格式 2，只包含训练样本的引用与抽样状态。
- 样本每批最多 256 条，已保存的数据可由多个模型存档共用，新增样本按批保存。
- 保留历史样本文件以支持旧轮次续训，不自动删除用户已有模型或数据。
- 兼容格式 1；在模拟规则兼容时可以直接读取，再次保存时使用新格式。
- 样本被清理后，保留模型和优化器，显示提示并重新收集样本。
- 训练中途停止后再清理样本，也能补充新对局并完成剩余训练更新。
- 数据文件先完整写入，再保存模型引用；写入失败不会破坏上一份模型存档。
- 整体移动文件夹仍能找到样本。校验失败会明确报错，原有规则版本检查继续生效。

## 实际验证

- 全部 465 项测试通过，失败 0，错误 0。
- 原存档的 130 条样本成功分离；模型、优化器、样本与抽样状态保持一致。
- 拆分前后同一局面的策略概率、价值预测和状态表示一致；原存档校验值未变。
- 原模型存档约 55.39 MB，拆分后模型部分约 51.02 MB，独立样本约 4.36 MB。
- 当前规则下完成两轮真实训练：战斗网络和编队网络各更新一轮；产生 162 条样本。
- 新模型重新读取后，模型参数、优化器和样本均一致，已验证可续训。
- 原生 macOS 桌面验收：平台 cocoa，模型加载、模拟、MCTS、训练、暂停、停止与新存档显示均通过，应用错误列表为空。
- 所有本次验证训练均已停止。

## 现有旧存档的兼容情况

本次检查使用的历史存档是技能规则版本 3，当前程序已是版本 4。
该差异在本次存储修改之前就已存在，因此没有将旧经验或旧优化器冒充为可直接续训的新数据。
历史存档只生成了保留原版本标记的拆分副本，用于验证存储一致性；
当前可运行的新存档通过迁移已有权重、使用当前规则重新收集样本并短训练生成。
本次验证说明功能正常，不代表通关能力已经提升。

## 可使用的文件

- 当前可续训存档：`checkpoints/separated_model/latest.pt`
- 配套配置：`configs/separated_model_resume.yaml`，加载后点击开始会继续两轮。
- 当前验证任务的训练数据：`训练数据/separated_model-acd98f93091d/`
- 清理说明：[训练数据/使用说明.txt](训练数据/使用说明.txt)
- 旧存档拆分验证副本：`outputs/replay_storage_verification/legacy_separated/latest.pt`，保留旧规则标记。
- 详细检查数据：`outputs/replay_storage_verification/runtime_verification.json`
- 桌面验收报告：`outputs/replay_storage_verification/desktop/acceptance.json`
- 全部测试记录：`outputs/replay_storage_verification/all_tests.log`
- 修改前源码备份：`outputs/replay_storage_verification/source_before.zip`

停止对应训练后，删除「训练数据」中对应任务的子文件夹即可清理样本，模型参数会保留。
历史格式的旧存档仍包含原来的样本，不会自动变小。回放容量限制当前训练样本量，
历史样本及日志的累计硬盘占用还需按任务清理。
