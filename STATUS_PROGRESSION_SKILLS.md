# 干员养成、技能审计与召唤物接入现状

2026-09-15。源码修改位于 `/Users/yihao/Desktop/arkzero/神经网络`。本轮没有启动正式训练。

**养成配置已接入对战和训练；全员技能与召唤物战斗效果尚未全部实现。** 以下明确区分数据覆盖、已实现行为和剩余工作。

## 已可用的养成设置

对战页点击 **干员养成**；训练页在 **高级配置与 YAML → 干员养成** 中设置。对战配置保存到 `configs/simulator_progression.json`；训练配置通过现有保存按钮保存到会话 YAML。两者独立，修改对战不会悄悄改写训练配置。

- 全员等级：精零 1 级、精一 1 级、精二 1 级、精二 90 级，也可自定义精英阶段和等级。
- 潜能：无提升至提升五次，对应游戏潜能 1–6。每次提升从该干员真实 `potentialRanks` 累计读取，不使用统一数值。
- 技能等级：Rank 1–7、专一、专二、专三。
- 携带技能：全员 S1 / S2 / S3，可逐人覆盖携带的技能。不存在或未解锁时使用首个已解锁技能，并在预览和战斗日志中明确提示。
- 实际属性预览在后台线程解析，展示精英、等级、潜能、技能等级、HP、ATK、DEF、DP 和当前限制。
- 中文、英文控件；对战进行中锁定养成设置。重开沿用原局设置。

统一目标按每名干员的合法上限处理。若目标精英阶段高于干员最高阶段，则采用其最高阶段满级，例如“全员精二 90”下黑角为精零 30、玫兰莎为精一 55。相同阶段则仅截到等级上限。技能 Rank 和专精根据 `allSkillLvlup` / `levelUpCostCond` 解锁条件限制，不允许精零直接专三。

已实现潜能属性加成包括费用、HP、攻击、防御、法抗、再部署时间、攻速等表中直接加算项。天赋增强和自定义效果没有因为选择了潜能就伪装实现，预览与日志逐项列出。信赖仍为 0，模组和未校准天赋不计入属性。

实际养成属性进入 `OperatorData`、GameState、状态键、模型观察和搜索。自主编队网络选择前的候选属性也按设置更新。回放保存每人最终养成设置及限制说明，重新加载可复现。

## 全员技能审计

当前已收录的 431 名干员中，17 名无技能；共有 949 个技能栏，对应 893 个去重技能。所有技能都有源表数据，包含描述、Rank/专精数值、技力、持续时间及解锁条件。

修改前仅 11 个技能具备简化效果。此次新增 21 个逐项检查过的有限时长、自身攻击/防御/攻速强化技能，合计 **32 个具备简化实现，861 个仍为 UNSUPPORTED**。例如星熊 S1、闪灵 S1、远山通用战术咏唱等可使用现有属性修饰器执行。实现清单集中在 `arknights_sim/data/skill_loader.py`。

不能仅凭黑板出现 `atk`、`def` 就自动当作普通强化：星熊 S3 有特殊群攻，麦哲伦技能涉及无人机，部分技能还包含多段攻击、锁血、自毁、索敌和队友光环。它们仍明确标记未实现。32 个技能的“简化实现”也不代表所属干员的全部特性、天赋和攻击方式已经还原。

**研究工具 → 游戏数据 → 技能** 现在显示全部 893 个技能，而非只显示已实现技能；每个 Rank/专精都列出完整描述、数值和独立的支持状态。干员详情中也显示对应技能支持状态。描述替换支持百分比和负数参数；无法解析的参数保留原占位符，不编造数值。

审计文件：

- `research/skills/coverage_before.json`：修改前 11 个简化技能的覆盖情况。
- `research/skills/coverage.json`：全员技能、每一级描述/数值、支持情况和召唤物关联。
- `scripts/audit_operator_skills.py`：可重复生成审计。

## IPA 复查

重新直接读取用户提供的 `com.hypergryph.arknights_2.7.71_und3fined.ipa` ZIP 内容和 metadata，没有运行游戏客户端。文件大小 2,681,039,975 字节；`global-metadata.dat` 大小 43,758,316 字节，metadata version 29；提取到 2,159 条包含 Skill/skill 的标识符。

未发现以 `skill_table` 或 `character_table` 命名的明文表条目。资产包中的表、脚本和热更新版本仍需另行分析。方法名称不能证明技能的真实执行规则；现有公开 GameData 与此 IPA 也不是同版本一致性证明。

证据：`research/skills/ipa_recheck.json`、`ipa_skill_identifiers.txt`。原 IPA 未修改。

## 训练中的技能操作

当前环境合法动作已有 `ACTIVATE_SKILL`；仅对已部署、存活、就绪、未激活且允许手动触发的技能产生动作。网络观察包含 SP、技能就绪/生效状态、剩余时间和已支持的技能参数；动态 ActionEncoder、PUCT、Self-play 和 Replay Buffer 都保留该动作。

本轮自动测试通过真实 `PolicyValueNetwork` 的策略梯度更新，让 PUCT 从就绪状态选择开启技能，再由环境执行并产生 `SKILL_START`；检查技能动作在激活后消失、父状态不变、经验池保存动作身份。这个人工构造的小型接线测试只证明动作可学习和执行，不代表学会正确开技能时机或通关。

训练 YAML 新增 `operator_progression` 和 `skill_overrides`，训练存档会保存它们；恢复训练采用存档中的养成配置。`skill_engine_version=2` 隔离旧效果语义下的训练经验：旧版存档不能直接续训，可用 `warm_start` 导入权重重新生成经验。推理权重仍可加载，但旧模型未针对新增技能训练。

`configs/skills_progression_verification.yaml` 是尚未启动的手动验证配置。

## 召唤物：已经核对的关系和未完成的运行时

源数据包含 60 名干员的 74 组召唤物/部署装置关联，不能误解为 60 名传统召唤师。**研究工具 → 游戏数据 → 召唤物** 已展示拥有者、技能绑定、召唤物成长表、天赋、技能及待实现项。

`resolve_summon_sources` 按实际精英、等级、潜能和携带技能选取对应源数据。例如麦哲伦 S1/S2/S3 对应三种无人机，潜能条件会选择正确天赋条目；深海色的触手技能槽存在空 skillId，不能伪造普通主动技能。

**召唤物放置、作战和技能联动尚未接入战斗环境，不能用于当前神经网络训练。** 当前阻碍是运行时基础设施未实现，而不是数据列表缺失：

1. GameState 需加入召唤师归属、独立实体 ID、可用库存、场上数量、共享冷却、部署位权重及回收状态。
2. 放置合法性需覆盖可部署地块、射程内战术点、召唤师是否在场、费用及占位；不能把所有召唤物当普通干员直接加入 12 人编队。
3. 作战需分别处理攻击、阻挡、被索敌、能否接受治疗、死亡和主人退场规则。
4. 召唤师与召唤物技能需有真实联动：强化/回血、爆炸回收、替换补充、隐匿、复制、合体等。每个类型需要独立可验证处理器。
5. 神经编码需要独立召唤物观察及部署/回收/技能动作，不能塞进现有固定编队槽后静默截断。网络格式迁移、回放、确定性和 clone 隔离都需配套验证。

下一项具体实现应先贯通深海色触手的完整生命周期与 S1 群体强化/回血，再扩展梅尔、麦哲伦、令、缪尔赛思等差异机制；没有把资料展示计作这些功能的完成。

## 验证与保存

基线完整测试为 305 passed；最终 **320 passed、0 failed，56.57 秒**，新增 15 项测试。最终结果见 `research/skills/tests_final.xml`；新增测试集中在 `tests/test_progression_skills.py`，覆盖全员养成上限、潜能数值、技能解锁、真实伤害和到期、神经技能动作、克隆、回放、训练存档、UI 后台预览和召唤物源数据选择。

`research/skills/progression_preview_zh.png` 是使用真实属性的离屏界面截图。原生启动冒烟检查使用 `scripts/run_desktop.py --quit-after 4`，不代表已经人工逐项点击验收。

迁移后的既有构建脚本将本机启动器放在项目同级：`/Users/yihao/Desktop/arkzero/Arknights Zero.app`；它加载本目录源码与 `.venv`。此次按该脚本重新构建并验证本地签名，未生成独立发行包。没有修改旧项目目录或启动正式训练。
