# 神经网络奖励机制检查

检查日期：2026-09-15。检查范围：当前训练代码、已停止的 overnight_joint 训练记录及机制验证版状态。本次没有修改奖励、模型或训练配置，没有启动训练。

## 结论

当前奖励信号在已有训练中缺乏区分度：22 个完整训练轮次的 66 局自我对战，战斗回报和编队回报全部为 -1。模型没有从这些回报中获得哪种操作或编队更好的直接区分信号。策略仍会根据搜索结果和探索项更新，不能说梯度完全为零，但损失降低并不代表学会通关。

## 战斗操作网络

- `training/self_play.py:123`：成功 +1，否则 -1；一局中所有记录的决策共用这个最终价值目标。
- `arknights_sim/environment/env.py:145`：成功必须同时满足战斗结束、底层结果 WIN、零漏怪、击杀数等于整关计划生成的敌人数。这里训练的是零漏怪全歼目标，比允许漏怪的普通通关严格。
- 失败、漏怪后的底层 WIN、时间上限和决策次数上限，均不能获得 +1。用户主动中断的未完成局被丢弃，不作为失败样本训练。
- `mcts/search.py:104`：搜索遇到终局，也使用同样的 +1/-1。
- `training/trainer.py:255` 和 `training/replay_buffer.py:180`：策略学习搜索产生的动作分布；价值学习最终回报，使用均方误差。现配置价值损失权重为 1，另外使用 AdamW 权重衰减。
- 没有对击杀、伤害、部署、撤退、使用装置、耗时或费用设置单独的过程奖励。

## 自动编队网络

`training/squad_selection.py:46`：成功 +1；否则：

```
编队奖励 = 限制到 [-1, 1]（2 × 击杀数 / 总敌人数 - 1 - 漏怪数 / 总敌人数）
```

总敌人数为整关计划生成数，不是已经出场数；分母至少为 1。采用带价值基线的策略梯度，价值误差系数 0.5，探索熵系数 0.02。全队选择共享局末回报，没有逐个干员的独立贡献奖励。训练池目前为 77 人，每次选 6 人。

用总数 10、敌人已全部被击杀或漏掉的假设场景说明：

| 战况 | 战斗回报 | 编队回报 |
| --- | ---: | ---: |
| 击杀 0、漏 10 | -1 | -1 |
| 击杀 3、漏 7 | -1 | -1 |
| 击杀 5、漏 5 | -1 | -0.5 |
| 击杀 9、漏 1 | -1 | +0.7 |
| 击杀 10、漏 0，成功结束 | +1 | +1 |

下限截断会把部分不同表现压成同一个 -1。若所有敌人都已被击杀或漏掉，击杀占比不超过三分之一时都是 -1；提前失败、还有敌人未出场的情况，不能直接套用这个三分之一阈值。

两套目标也存在差异：高击杀但漏怪、或接近通关时达到时间/决策上限，编队可能获得较高回报，战斗仍为 -1。这是代码层面的潜在目标不一致；本次 66 局没有时间或决策上限终止，不能称为已发生的超时漏洞。

## 实际记录

来源：`outputs/overnight_joint/episodes.jsonl`、`evaluations.jsonl`、`training_metrics.jsonl`、`status.json`。

- 完整轮次：22；自我对战：66 局；成功：0；每局击杀 0～1，合计击杀 3。
- 66 局战斗回报全部 -1；66 局已记录的编队回报也全部 -1。
- 自我对战全部自然结束，不是因时间或决策次数限制被截断。
- 记录了 469 次部署、466 次撤退、12401 次等待；208 次部署后紧接同一时刻撤退同一干员。该行为值得检查，但这些记录不足以证明奖励是唯一原因。
- 固定评估：初始评估加 22 轮评估，共 69 局，成功 0 局；22 轮没有候选模型获选替换最佳模型。
- 第 22 轮记录的搜索平均价值为 -1，价值损失约 0.000000417。这与学到普遍失败的预测一致，不能解读为掌握战斗。
- 自我对战使用保留的最佳战斗模型；候选模型按固定评估决定是否替换。评估排序依次比较成功率、平均回报、较少漏怪、较多击杀；这是模型选拔规则，不会给每步训练额外奖励。
- 以上仅统计落盘的完整轮次及评估记录，不把停止时可能存在的待完成轮次混入 66 局。

## 建议，尚未实施

1. 先明确普通通关和零漏怪通关的奖励层级，统一战斗与编队的最终目标。
2. 给击杀、漏怪等真实战斗进展提供有界且能区分早期表现的反馈；避免下限过早压平。若引入过程奖励，需要同步设计搜索回传和训练目标，不能只改编队公式。
3. 不按部署次数、伤害次数或按开关次数直接发奖，避免反复部署撤退、拖延或操作装置刷分。机制操作应由它带来的战斗效果评价。
4. 同时检查立即撤退、搜索深度、探索和训练课程。奖励不是唯一因素，不能承诺改公式就能通关。
5. 用固定关卡和种子验证胜率、零漏怪率、击杀/漏怪、反复部署撤退及超时情况，分别记录奖励分布。

新加入的重力、侦测器和障碍物已接入模型观察与动作表示，但没有新增独立奖励，且机制验证模型尚未接受这些机制的训练。

## 保存与暂停状态

`outputs/overnight_joint/status.json` 为 STOPPED，原因 user_requested；记录的训练进程 PID 98165 已不存在。`outputs/mechanics_verification/status.json` 为 PAUSED_FOR_VERIFICATION，training_started=false。原有验证快照保持不变，等待用户验证。
