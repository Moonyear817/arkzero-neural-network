> 更新：账号主人已确认转录，乌尔比安潜能为游戏一潜（内部 0）。已保存 71 名近卫正式快照；下文旧版待确认记录仅保留历史。信赖属性及账号战斗输入绑定已补入，最新状态见 STATUS_CHAPTER8_FOUNDATIONS.md。

# 视频账号练度整理与训练接入状态

2026-09-15。已整理用户视频并接回开发版账号核对入口；**没有启动账号第八章训练，没有生成虚假的人工确认快照**。

## 本次保存的数据

- 从视频识别并逐页核对 **379 名可见干员，71 名近卫**。这是视频覆盖范围，不是对视频外持有情况的断言。
- 104 名精二、35 名精一、240 名未精英化。保留实际等级，不默认满级。
- 技能等级、每个技能的独立专精、当前携带技能分别记录。阿米娅按视频显示的术师形态记录，不推断其他形态解锁。
- 记录 55 名干员当前装备模组，其中近卫 9 名。模组保存具体 ID、名称、类型、等级；画面中其他已解锁但未装备的模组单独记录，不叠加生效。
- 所有干员信赖按用户明确要求覆盖为 **200%**，来源标记 `USER_OVERRIDE_FULL_TRUST`。能读到的原始信赖单独保留，无法读取时为空。
- **25 名的潜能仍待确认，其中近卫仅乌尔比安一名。** 其他近卫字段通过现有结构与解锁范围检查，但这不替代账号主人的核对。
- 面向用户的完整清单显示游戏内潜能1–6；项目 JSON 保存提升次数0–5。模糊图标保持 null，不凭稀有度或练度猜潜能。
- 原视频已从微信临时目录复制到项目并保存 SHA-256，避免临时附件清理后丢失。每条记录包含对应视频帧、近似时间与原始行截图。

## 文件与入口

- `outputs/guards_chapter8/account_video/roster_review.html`：可搜索、筛近卫、筛待确认项的完整清单，支持查看逐行视频证据。
- 同目录 `roster_review.md` / `roster_review.json`：完整文字版和结构化资料。
- 同目录 `guards_draft.json`，以及 `data/accounts/drafts/video_*_guards.json`：现有账号窗口可读的71人近卫草稿。
- `data/accounts/drafts/pending_video.json`：待核对草稿入口。
- `configs/account_video_session.yaml`：R8-1、账号近卫范围、16次搜索、单环境、信赖覆盖200%、未确认快照。
- `outputs/guards_chapter8/account_video/training_preflight.json`：按这份账号练度执行的准入审计。
- `data_consistency.json`：379条等级和专精解锁范围检查记录。
- `source/account.mp4` / `source/manifest.json`：保留的用户视频与摘要；`evidence/`、`rows/` 为读取证据。

开发版：`python scripts/run_desktop.py --config configs/account_video_session.yaml --review-account`。

训练页 → 高级 → 账号截图与核对 → 视频整理结果，可再次导入。加载本次配置时自动预填草稿。“查看完整角色清单”打开人类可读资料。导入不会静默覆盖正在编辑的行，重复导入须删除重复行后确认。

## 接入与修复

- 核对窗口显示干员姓名，保留内部ID、独立专精、携带技能、模组和视频证据。修改可见字段、保存或确认时不丢弃这些额外资料。
- 人工确认后才写内容寻址的正式快照；确认不会改变原草稿。仍有未知必填字段时拒绝确认。
- 校验携带技能是否属于该干员且已解锁。
- 补齐项目原数据提交 `0ef7f952dfd018392200157a5c79a6511ba69122` 的 `uniequip_table.json` 和 `favor_table.json`，记录摘要并加入数据获取清单。
- 修复模组信赖校验：真实资料使用 `unlockFavors` 原始点数，不能把该点数与信赖百分比直接比较。按同版本 favor 表换算，并检查模组归属、等级和精英等级门槛。
- 账号快照或账号草稿配置缺少明确训练范围时直接拒绝，避免悄悄按默认队伍、统一练度训练。

## 当前训练阻碍

本次审计不把草稿当作已确认快照，也不把“资料能读取”当作战斗效果已验证。

1. 账号草稿需要用户确认；乌尔比安潜能尚缺。
2. 71名近卫共154个技能条目；其中101个在当前练度已解锁，**90个独立技能ID仍被现有技能 Loader 明确判定为未实现**。另外11个被Loader接受不等于完整机制已验证。
3. 模组效果、信赖属性、完整特性与天赋校准尚未完成；当前就绪注册仍保守关闭。
4. 审计第八章普通战斗17条记录，当前没有可正式开放的关卡。R8-1的首个具体解析阻碍为 `trap_020_roadblock`。此项不是关卡全部缺口的穷尽清单。
5. 玩家可见观测隔离、账号异构练度绑定、自主选队与携带技能选择及快照与训练存档绑定仍需实现。

因此没有运行默认0-1替代实验，没有用损失下降或简化战斗宣称按此账号练度完成第八章训练。没有连接PlayCover。

## 验证

- 原有基线400项测试；之前账号阶段增加15项；本次增加5项：**完整420 passed / 0 failed，68.66秒**。证据：`tests_full.xml`。
- 账号与桌面定向：32 passed，4.83秒，`tests_targeted.xml`。
- 379条转录数据通过等级/专精结构范围检查；71名近卫中70条通过完整账号字段校验，乌尔比安因潜能未知被正确拒绝。
- 账号核对窗口真实加载71条，往返保存保留元数据；截图：`account_dialog.png`。
- 原生开发版加载本次配置，5秒后正常退出，退出码0。系统输出一条输入法IMK日志，没有导致失败。
- 最后再次打开开发版账号核对入口，供用户核对；未自动按下确认或开始训练。

下一步先由用户确认视频转录，锁定正式版本；随后按确认的近卫、技能与模组范围补机制及R8-1场景，再开启有界训练验证。
